apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: microservices
spec:
  selector:
    app: notification
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP